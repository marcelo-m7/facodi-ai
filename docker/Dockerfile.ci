FROM odoo:19.0

USER root
COPY requirements.txt /tmp/facodi-ai-requirements.txt
RUN python3 -m pip install \
    --no-cache-dir \
    --break-system-packages \
    --ignore-installed \
    -r /tmp/facodi-ai-requirements.txt \
    && python3 -c "import pydantic_ai"
USER odoo
